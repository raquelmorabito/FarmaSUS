spring:
  application:
    name: medication-service
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration
  datasource:
    url: jdbc:postgresql://postgres-medication:5432/medicationdb
    username: farma
    password: farma
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: America/Sao_Paulo
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: farma
    password: farma
    messaging:
      exchange:
        events: ${RABBITMQ_EXCHANGE_EVENTS:farma.events}
      routing:
        medication-created: ${RABBITMQ_ROUTING_MEDICATION_CREATED:medication.created}
        medication-prescription-expiring: ${RABBITMQ_ROUTING_MEDICATION_PRESCRIPTION_EXPIRING:medication.prescription.expiring}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

jwt:
  secret: ${JWT_SECRET}

jobs:
  receita-perto-vencer:
    cron: ${RECEITA_PERTO_VENCER_CRON:0 0 8 * * *}
    zone: ${RECEITA_PERTO_VENCER_ZONE:America/Sao_Paulo}
    dias-antecedencia: ${RECEITA_PERTO_VENCER_DIAS:7}

services:
  safety:
    url: ${SAFETY_SERVICE_URL:http://safety-service:8081}

app:
  seed:
    paciente-login:
      mappings:
        ${SEED_PACIENTE_LOGIN_1}: ${SEED_PACIENTE_ID_1}

http:
  client:
    connect-timeout-ms: ${HTTP_CLIENT_CONNECT_TIMEOUT_MS:2000}
    read-timeout-ms: ${HTTP_CLIENT_READ_TIMEOUT_MS:5000}
