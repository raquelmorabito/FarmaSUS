spring:
  application:
    name: adherence-service
  datasource:
    url: jdbc:postgresql://postgres-adherence:5432/adherencedb
    username: farma
    password: farma
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: America/Sao_Paulo
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: farma
    password: farma
  jackson:
    time-zone: America/Sao_Paulo

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

server:
  port: 8084

jwt:
  secret: ${JWT_SECRET:alterar-este-segredo-para-producao-uma-chave-longa}
